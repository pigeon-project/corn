(macro let [
    (_ ([var expr] ...) body ...)
    ((lambda (var ...) body ...) expr ...)])

(macro if [
    (_ cd then else)
    (cond
        [*cd *then]
        [#t *else])])

;(macro match (ast)
;    (transport ast))

;(macro match [
;    (_ expr [pattern result] ...)
;    (transport )])
